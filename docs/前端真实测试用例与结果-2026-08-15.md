# 前端真实测试用例与结果（2026-08-15）

## 测试边界

- 项目：年报问答 RAG V1。
- 页面：`http://127.0.0.1:3000/` 与 `http://127.0.0.1:3000/?view=brief`。
- 浏览器：Codex 内置真实浏览器。
- 桌面视口：`1446 x 792`。
- 移动视口：`390 x 844`。
- 后端：`http://127.0.0.1:8001/`。
- 活动集合：`annual_report_v1_single_company`。
- Planner 与 Answer：`deepseek-v4-flash`，thinking disabled。

## 本次修改

- 检索与生成过程从可折叠 `details` 改为固定显示 `section`。
- 流程条移到线索页主内容顶部并跨越两列。
- 证据原文和来源索引进入同一两列网格，桌面端顶部对齐。
- 检索详情和本地服务保留在下方独立区域。
- 修复有真实数据时空状态仍显示的问题；`hidden` 状态现在对应 `display: none`。

## 修改前基线

桌面端有数据状态：

| 区域 | x | y | width | height |
| --- | ---: | ---: | ---: | ---: |
| 检索与生成过程 | 30 | 178 | 678 | 3704 |
| 证据原文 | 722 | 178 | 678 | 3704 |
| 来源索引 | 30 | 3895 | 594 | 949 |

流程面板被同一网格行的证据面板拉伸到 `3704px`，来源索引落在下一行，无法与证据原文对齐。

## 真实问题与结果

问题：

```text
塔塔咨询服务公司2024财年的营业收入和总收入分别是多少？
```

真实回答关键值：

- 营业收入：`2,40,893` INR crore。
- 总收入：`2,45,315` INR crore。
- 其他收入：`4,422` INR crore。
- 页面状态：`引用已验证`。
- 检索：`hybrid · 6 条命中`。
- 主题覆盖：`2/2`。
- 证据原文：6 条。
- 来源索引：6 条。
- 所有证据均属于 `Tata Consultancy Services Limited`。

## 测试用例与结果

| ID | 用例 | 操作 | 期望 | 结果 |
| --- | --- | --- | --- | --- |
| FE-BRIEF-01 | 页面身份与真实内容 | 打开 `/?view=brief` | 页面标题正确，非空白，无框架错误覆盖层 | 通过 |
| FE-BRIEF-02 | 流程条固定显示 | 检查 `.process-panel` | 标签为 `SECTION`，没有 `summary`，始终展示三步状态 | 通过 |
| FE-BRIEF-03 | 流程条跨两列置顶 | 桌面端测量区域 | 宽度覆盖主网格两列，高度不被证据内容拉伸 | 通过，`1371 x 127` |
| FE-BRIEF-04 | 证据与来源对齐 | 有数据状态测量顶部坐标 | 两个区域桌面端 `y` 相同 | 通过，均为 `y=419` |
| FE-BRIEF-05 | 空状态隐藏 | 检查 `#evidence-empty`、`#citation-empty` | 有数据时 `hidden=true` 且 `display=none` | 通过 |
| FE-BRIEF-06 | 来源定位证据 | 点击来源索引 `[S3]` | 页面滚动到 `#evidence-S3` | 通过，目标存在且位于视口内 |
| FE-BRIEF-07 | 返回问答页 | 点击“返回问答界面” | 回到问答视图，真实答案和引用状态保留 | 通过 |
| FE-BRIEF-08 | 移动端布局 | 设置 `390 x 844` | 单列排列，无横向溢出，文本和按钮不重叠 | 通过 |
| FE-BRIEF-09 | 控制台健康 | 读取 warning/error | 无页面 warning/error | 通过，`[]` |

结果：`9/9` 通过。

## 修改后测量

桌面端有数据状态：

| 区域 | width | height | 顶部关系 |
| --- | ---: | ---: | --- |
| 检索与生成过程 | 1371 | 127 | 独立第一行，跨两列 |
| 证据原文 | 678 | 4140 | 与来源索引同为 `y=419` |
| 来源索引 | 678 | 670 | 与证据原文同为 `y=419` |

移动端：

- 视口：`390 x 844`。
- 页面内容宽度：`375`，未超过视口。
- 流程区：`347 x 272`。
- 证据原文与来源索引宽度均为 `347`，按单列顺序排列。
- `pageOverflow=false`。

## 截图证据

- 桌面端：`前端真实测试-线索对齐-2026-08-15.png`。
- 移动端：`前端真实测试-线索移动端-2026-08-15.png`。

## 结论

本次修改解决了流程面板被证据内容等高拉伸的问题。流程状态固定显示在顶部，证据原文和来源索引在桌面端同一行对齐；移动端按单列展示且没有横向溢出。真实 DeepSeek Flash 问答、引用校验、来源定位与返回问答页均已通过。
