# V1 上传内容清单

更新日期：2026-08-17

目标目录：`F:\新建文件夹\Rag_v1`

## 上传边界

本目录只保留年报问答 RAG V1 的可运行源码、前端、活动数据库、7 份实际入库源文件、2 份已保留切块对应的补充原始 PDF、活动切块、固定题集和最终验收材料。早期学习数据、旧数据库、旧集合、中间解析结果、本地环境和真实密钥不复制。

当前确定的首次上传类型是“私有完整包”：GitHub 仓库必须先设为 `Private`，当前候选可直接进入首次提交。当前目录不是已经审完第三方再分发权限的“公开作品集包”。

若仓库要设为 `Public`，不要直接执行 `git add --all`。必须先另行移除或确认以下内容的许可：

- 9 份 PDF/DOCX/XLSX 原始文件，其中 7 份属于活动集合，2 份是补充原件。
- `data/processed/annual_reports/v1_single_company_chunks/` 派生原文。
- 包含完整 Evidence 原文的 acceptance/retrieval JSON。
- `xianlian/` Qdrant 数据库。
- `models/bge-m3/` Tokenizer。

公开瘦身时还要重新处理 CI 所需的测试夹具，避免为了删除第三方派生数据而使 `python -m unittest discover -v` 失效。

## 源码

- `chunking/`
- `cleaners/`
- `embeddings/`
- `parsers/`
- `prompts/`
- `rag_api/`
- `scripts/`
- `frontend/`
- `requirements-rag.txt`
- `.env.example`
- `.gitattributes`
- `.gitignore`
- `README.md`
- `GitHub上传审核信息.md`
- `LICENSE`
- `THIRD_PARTY_NOTICES.md`
- `.github/workflows/unit-tests.yml`

## 活动数据库

- `xianlian/meta.json`
- `xianlian/collection/annual_report_v1_single_company/storage.sqlite`

数据库只登记 `annual_report_v1_single_company`，不包含旧的 `annual_report_tcs_real_multiformat_v2` 回退集合。

活动集合验收值：

- Collection：`annual_report_v1_single_company`
- Vector size：`1024`
- Distance：`Cosine`
- Points：`3959`
- 数据库文件约 `61.0 MB`

`storage.sqlite` 超过 GitHub 网页上传的常用单文件范围，应使用本地 Git 命令提交和推送。

当前 Git 忽略规则生效后的发布规模：

- 可提交文件：197 个。
- 合计：147,107,428 字节。
- 最大文件：`storage.sqlite`，61,026,304 字节。
- 没有文件达到 GitHub 普通 Git 的 100 MiB 单文件硬限制。

`storage.sqlite` 超过 GitHub 建议的 50 MiB 阈值，推送时可能收到大文件警告，但仍低于 100 MiB 硬限制。当前 V1 明确要求携带活动向量数据库，因此保留普通 Git 提交；不要误把被忽略的 569 MB ONNX 加入提交。

## 实际入库源文件

### PDF 5 份

- `data/annual_reports/multicompany_eval_v1/raw/pdf/apple_2024_10k.pdf`
- `data/annual_reports/multicompany_eval_v1/raw/pdf/microsoft_2023_annual_report.pdf`
- `data/annual_reports/real_multiformat_v2/raw/pdf/tcs_2022_annual_report.pdf`
- `data/annual_reports/real_multiformat_v2/raw/pdf/tcs_2023_annual_report.pdf`
- `data/annual_reports/real_multiformat_v2/raw/pdf/tcs_2024_annual_report.pdf`

### DOCX 1 份

- `data/annual_reports/real_multiformat_v2/raw/docx/tcs_fy2022_2024_equity_research_report.docx`

### XLSX 1 份

- `data/annual_reports/real_multiformat_v2/raw/xlsx/tcs_fy2022_2024_financial_analysis.xlsx`

以上 7 份文件与活动切块中的 `source_file` 一一对应。来源 URL 和版权说明见 `data/SOURCES.md`。

## 补充原始 PDF

- `data/annual_reports/raw/apple/2022/apple_2022_10k.pdf`
- `data/annual_reports/raw/apple/2023/apple_2023_10k.pdf`

这两份原件与 `data/processed/annual_reports/pdf_chunks/` 中已保留的 Apple 2022、Apple 2023 切块哈希一致，不属于当前活动集合的 3959 points。

## 活动切块与构建记录

- `data/processed/annual_reports/v1_single_company_chunks/`：7 个 JSONL，3959 个 chunks。
- `data/processed/annual_reports/v1_single_company_reports/embedding.manifest.json`。
- `data/evaluation/v1_single_company.collection_build.json`。

为保证克隆后的 `unittest discover` 可以完整运行，另保留两组小型测试夹具：

- `data/processed/chunks/bge-m3-tokenizer-v1/`。
- `data/processed/annual_reports/pdf_chunks/`。

这两组文件只供单元测试使用，不属于活动集合的 3959 个 chunks。

构建记录中的绝对路径是原始验收环境的审计字段，发布副本保留原值，不代表克隆后必须使用相同目录。

## 固定题集与最终报告

- `data/evaluation/test_set_v1_1.jsonl`：V1.2 契约继承的 21 道基础题，缺少时评测器无法加载。
- `data/evaluation/test_set_v1_2.jsonl`
- `data/evaluation/test_set_v1_2.acceptance.json`
- `data/evaluation/test_set_v1_2.retrieval.json`
- `data/evaluation/test_set_v1_chinese_aliases.jsonl`
- `data/evaluation/test_set_v1_chinese_aliases.acceptance.json`
- `data/evaluation/test_set_v1_chinese_aliases.retrieval.json`
- `data/evaluation/tcs_pdf_qdrant_content_audit.json`

`docs/` 保留最终边界、准确率判读、数据库/PDF 核验、中文别名和真实前端测试记录及截图。

新增真实前端布局回归材料：

- `docs/前端真实测试用例与结果-2026-08-15.md`：9 个真实浏览器用例及结果。
- `docs/screenshots/前端真实测试-线索对齐-2026-08-15.png`：桌面端流程条、证据原文和来源索引对齐证据。
- `docs/screenshots/前端真实测试-线索移动端-2026-08-15.png`：`390 x 844` 移动端证据。
- `docs/前端手工提问问题合集.md`：39 道可直接粘贴到前端的固定回归、扩展、自然表达和边界/拒答问题。
- `docs/V1问题案例与测试结果边界.md`：统一记录代表性通过案例、历史失败、中文纯召回限制和测试结果解释。
- `docs/Rag与Rag_v1差异清单.md`：工作目录和发布副本的源码、配置及关键产物对比。
- `GitHub上传审核信息.md`：仓库创建选项、上传边界、提交/tag/Release 信息和推送命令。

该轮真实问题为“塔塔咨询服务公司2024财年的营业收入和总收入分别是多少？”，返回 `2,40,893 / 2,45,315` INR crore，页面状态为“引用已验证”，6 条证据与 6 个来源全部属于 TCS。

上述结果是已有验收记录。本次 2026-08-17 发布补充不重新执行真实用户问答，也不消耗 DeepSeek；完成后只启动服务供维护者本人测试。

## 自动化检查

`.github/workflows/unit-tests.yml` 在 push 和 pull request 时执行：

- 加载 V1.2 契约并确认基础题集为 `test_set_v1_1.jsonl`、合并后为 21 题。
- 执行 `python -m unittest discover -v`。

CI 不提交问题、不调用 DeepSeek，也不要求模型权重或本地向量数据库。固定题集完整评测仍需在本机明确执行。

2026-08-17 使用 F 盘已有依赖完整的 Python 3.13 虚拟环境在当前发布副本复跑：`Ran 63 tests`，`OK`。本轮没有重新运行 21 题、6 题完整评测或真实前端回归，详细边界见 `docs/V1问题案例与测试结果边界.md`。

## 本机运行补充

当前 `F:\新建文件夹\Rag_v1` 已为本机启动复制：

- `.env`：真实密钥配置，只留在本机，已由 `.gitignore` 排除。
- `models/bge-m3-embedding/model_quantized.onnx`：569,694,530 字节，只留在本机，已由 `.gitignore` 排除。

这两项不能进入 Git 提交。公开仓库的使用者需要自行创建 `.env` 并按 `models/README.md` 获取匹配权重。

## 模型文件

保留：

- `models/bge-m3/` Tokenizer 文件。
- `models/README.md`。

不上传：

- `models/bge-m3-embedding/model_quantized.onnx`，文件为 `569,694,530` 字节，超过 GitHub 普通 Git 的 100 MiB 限制。

运行前按 `models/README.md` 放置权重并校验 SHA-256。

## 明确排除

- `.env` 和任何真实 DeepSeek Key。
- `.venv_rag/`、缓存、日志、临时文件、`.codegraph/`。
- `models/bge-m3-embedding/model_quantized.onnx`。
- 旧 Qdrant 集合和旧数据库。
- 与当前 7 份活动来源和 2 份补充原件无关的数据集。
- 早期原型、重复阶段记录和已经被最终报告替代的文档。

## 第三方内容风险

根目录 MIT License 只覆盖本项目自行编写的代码和文档，不覆盖 7 份活动来源、2 份补充原件、Tokenizer、派生切块和向量数据库。来源、SHA-256 与再分发提醒见 `data/SOURCES.md` 和 `THIRD_PARTY_NOTICES.md`。公开上传前应由仓库维护者确认这些第三方内容的再分发权限；无法确认时不要提交第三方原文件及其派生数据。

## 对外描述

可以描述为：

> 支持 Apple、Microsoft、TCS 的单公司年报问答；完成 PDF、DOCX、XLSX 解析清洗、BGE-M3、Qdrant Local、BM25 + Dense Hybrid/RRF、DeepSeek Flash 查询规划、引用校验和固定题集评测。V1.2 每家公司 7 题通过，中文别名题集三家公司各 2 题通过。

不得描述为跨公司比较、任意年报通用系统或开放问题 100% 准确。
